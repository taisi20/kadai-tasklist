<%= form_with(model: task, local: true) do |f| %>
<!--エラーメッセージ-->
<% if task.errors.any? %>
<div id="ellor_exalanation">
  <ul>
    <% task.errors.full_messages.each do |msg| %>
    <li>
      <%= msg %>
    </li>
    <% end %>
  </ul>
</div>
<% end %>
<!--/エラーメッセージ-->
<%= f.label :content, "タスク" %>
<%= f.text_field :content %>
<%= f.submit "登録" %>
<% end %>